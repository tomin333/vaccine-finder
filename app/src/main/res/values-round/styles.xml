<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryThemeColor</item>
        <item name="colorPrimaryDark">@color/primaryThemeColor</item>
        <item name="colorAccent">@color/primaryThemeColor</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/primaryFocusedColor</item>
        <item name="android:navigationBarColor">@color/primaryFocusedColor</item>
    </style>

</resources>
